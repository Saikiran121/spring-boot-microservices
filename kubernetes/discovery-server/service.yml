apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  labels:
    app: discovery-server
spec:
  type: LoadBalancer
  selector:
    app: discovery-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
